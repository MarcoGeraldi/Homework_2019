module NOME_CIRCUITO_COMB(
	input a, b[44]
	input c 
	output x, y
);
assign x = c OR (a AND b)
assign y = NOT c
endmodule

module NOME_CIRCUITO_COMB(
clk
	input e, d
	output c, M
	input m
);

assign x 
FF5 
endmodule