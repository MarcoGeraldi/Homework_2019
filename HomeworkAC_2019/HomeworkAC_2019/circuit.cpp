#include "circuit.h"

circuit::circuit(const std::string &_name,
	const std::vector<signal_input> &_input_string,
	const std::vector<signal_output>&_output_string
)
{/*
	this->name = _name;
	for (int i = 0; i < _input_string.size(); i++)
	{
		this->circuit_inputs.push_back(_input_string[i]);
	}
	for (int i = 0; i < _output_string.size(); i++)
	{
		this->circuit_outputs.push_back(_output_string[i]);
	}*/
}